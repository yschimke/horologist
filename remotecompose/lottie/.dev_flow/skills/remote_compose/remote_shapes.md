# Remote Shapes

Remote Compose uses `RemotePath`, `RemoteCanvas`, and `RemotePaint` for drawing.
Operations are similar to Android `Path`, but wrapped in restricted APIs.
Use `RemoteModifier.drawWithContent` for custom drawing.
