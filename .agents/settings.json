{
  "permissions": {
    "allow": [
      "command(jj log)",
      "command(jj diff)",
      "command(jj status)",
      "command(jj show)",
      "command(/usr/bin/jj log)",
      "command(/usr/bin/jj diff)",
      "command(/usr/bin/jj status)",
      "command(/usr/bin/jj show)",
      "command(git log)",
      "command(git diff)",
      "command(git status)",
      "command(git show)",
      "command(git grep)",
      "command(ls)",
      "command(find)",
      "command(grep)",
      "command(awk)",
      "command(cat)",
      "command(/bin/cat)",
      "command(head)",
      "command(tail)",
      "command(mkdir)",
      "command(touch)",
      "command(cp)",
      "command(rm)",
      "command(patch)",
      "command(sed)",
      "command(./gradlew)",
      "command(../../gradlew)",
      "command(python)",
      "command(python3)",
      "command(echo)",
      "command(sleep)",
      "command(jq)",
      "command(sort)"
    ],
    "deny": [
      "command(sudo)",
      "command(rm -rf)",
      "command(git push)"
    ]
  }
}
